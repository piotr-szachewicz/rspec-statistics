require 'spec_helper'

describe Rspec::Statistics do
  it 'has a version number' do
    expect(Rspec::Statistics::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
